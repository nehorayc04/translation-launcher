**ביקורת אבטחה + הידוק ל-Game translator (2026-07-20) — ✅ הושלם**
- **בוצע:** ביקורת 4-מוקדים על `C:\Users\Nehoray_Cohen\Projects\Game translator` — hardcoded creds / תלויות / הרשאות תיקיות פלט / scoping של TEMP. `npm audit` = **0 פגיעויות** (248 חבילות), `pip-audit` על `translation_manager/requirements.txt` = **0 known**. אין tokens/JWTs/AWS/GH keys בקוד המקור (grep רחב על `sk-*`, `AKIA*`, `ghp_*`, `eyJ*`, `AIza*`, `TOKEN=…`). `.env` + `.env.build` היו **gitignored + מעולם לא committed** (אימות `git ls-files --error-unmatch` + `git log --all -- .env`).
- **הבעיה שאותרה + תוקנה:** `.env` ו-`.env.build` נשאו ACE של `CodexSandboxUsers:ReadAndExecute` שהגיע ב-**ירושה** מהתיקייה. פירושה: כל תהליך שרץ תחת זהות ה-sandbox של Codex/Claude יכול היה לקרוא את `KV_REST_API_TOKEN` (Upstash) + `MONITOR_TOKEN`. תוקן: `icacls .env /inheritance:d` (הופך ירושה לאקספליציטית) → `icacls .env /remove "CodexSandboxUsers"` (רק אחרי `/inheritance:d` זה תופס). אימות סופי: `.env` נשא רק SYSTEM/Administrators/Nehoray Cohen — כולם FullControl. גיבויים: `env_backup.acl`, `env_build_backup.acl` בשורש הפרויקט (להעביר מחוץ ל-repo או למחוק כשהמצב יציב).
- **🔴 THE gotcha אוניברסלית (Windows ACL):** `icacls <file> /remove:g "<principal>"` **מסיר רק ACE אקספליציטי, לא inherited** — מדווח "Successfully processed" גם כשלא עשה כלום, ולכן הריצה הראשונה של המשתמש נראתה מוצלחת אבל לא שינתה כלום. הרצף הנכון: `/inheritance:d` (ממיר ירושה→אקספליציטי, שומר את ה-ACEs הקיימים) ואז `/remove`. **אימות חובה: `Get-Acl <file> | Select-Object -Expand Access`** — לא לסמוך על ה-exit code / "Successfully processed" של icacls.
- **מה נמצא כ-לא-סיכון (מתועד לעתיד):** (a) `translation_manager/auth/_baked.py` מכיל `SUPABASE_ANON_KEY = "sb_publishable_..."` — פורמט publishable של Supabase, **מיועד ל-embed בבינארי**, מוגן ע"י RLS (המפתח המסוכן = service-role, יושב רק ב-Vercel functions של האתר). `auth/config.py` שורות 1-17 מתעדות את זה מפורשות. לא לחשוב שזה דליפה. (b) אין שימוש ב-`shell=True` בשום subprocess (0 hits) → אין shell-injection. `main_eel.py:590` = `subprocess.Popen([str(exe)], cwd=str(exe.parent))` — list args, בטוח. (c) ACLs של תיקיות פלט (`steam_hebrew_output/`, `frontend/dist/`, `progress_monitor/`) נקיים — אין הרשאות ל-`Everyone`/`Users`.
- **הידוק דחוי (רשום, לא הופעל):** 14 קבצי Python מקודדים-קשה `C:\Users\Nehoray_Cohen\AppData\Local\Temp\<subdir>` (למשל `rebuild_onscreens_and_pack.py:48`, `cp2077_subtitle_batch.py:51`, `progress_monitor/adapters/cp2077.py:49`, `forensic_extract_deployed.py:25`, 4 קבצי `heebo`/`font_v2`, `audit_all_missing_translations.py:70` ועוד). לא סיכון אבטחה (per-user TEMP), אבל היגייני/portability (שם המשתמש `Nehoray_Cohen` צרוב לתוך הקוד). **המעבר הבטוח: `Path(tempfile.gettempdir()) / "<subdir>"` — לא `mkdtemp()`.** כמה מהנתיבים האלה הם **חוזה משותף בין סקריפטים** (סקריפט A כותב ל-`reextract_subs\text`, מוניטור B קורא ממנו): רנדומיזציה שוברת את הצינור, ו-`TRUTH_CACHE` ב-`audit_all_missing_translations.py` הוא cache **מכוון-להישאר** בין ריצות. חייבים להגר בצוותים בקוראים אחת: (1) onscreens pipeline (קובץ בודד) → (2) subtitles pipeline (3 קבצים חייבים לזוז יחד) → (3) font tooling (4 חולקים `font_v2`) → (4) one-shots (6). `git commit` לפני כל קבוצה, ולעולם לא במהלך ריצת תרגום פעילה (`translate_queue_fast.py` מחזיק קבצים פתוחים).
- **היגיינה שהתגלתה אגב (לא אבטחה):** ל-`.env` יש שורת `MONITOR_TOKEN=...` **כפולה** (שורות 3 ו-5, זהות). לא מזיק (Python לוקח את האחרון), אבל למחוק בעריכה הבאה.

