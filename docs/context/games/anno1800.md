## Anno 1800 purchasable (₪53) + installable in the launcher (2026-06-23)

Made the Anno 1800 Hebrew mod a **paid download-mod** (₪53 / 5300 agorot) installable
through the launcher — reusing the existing generic paid-mod machinery (buy button +
`owns_game` DRM gate + install/update + website Buy) with **no new RPCs and no new
GameDetailPanel branch**. The one twist: Anno is a **loose-file mod** that deploys into
`%Documents%\Anno 1800\mods\` (NOT the game folder; the zip already nests
`zzz_hebrew_translation/` at top level).

- **The seam — `_deploy_root(game_id)` (main_eel.py).** A game whose `GameConfig` has the
  new `documents_subdir` (Anno = `r"Anno 1800\mods"`) deploys to `_documents_dir()/sub`;
  every other game returns `_install_path` **byte-identical** (CP2077 untouched).
  `_documents_dir()` resolves the real Documents known-folder (HKCU `…\Shell Folders\Personal`
  via winreg, OneDrive-safe; falls back to `~/Documents`). `_deploy_root` is threaded into the
  FILE-TARGET sites only — `_mod_state` (`detect_state`), `get_game_mod_state`/`check_game_mod_update`/
  `get_mod_updates` (`status`), `_run_game_mod_install` + `set_game_mod_installed` (`install`/`disable`,
  with a `mkdir(parents,exist_ok)` of the deploy root for documents mods), `clear_game_mod_cache`.
  The DRM gate + `base is None` "game detected" check + `hasPath` stay on `_install_path`.
- **`config.py`:** new `GameConfig.documents_subdir` field + an Anno entry (`mod_slug="anno1800-hebrew"`,
  `mod_files=[r"zzz_hebrew_translation\modinfo.json"]` = installed-detection sentinel, common_paths +
  `validation_file=r"Bin\Win64\Anno1800.exe"`). `game_mod.py` unchanged (already takes an arbitrary root).
- **Activation (auto + note).** Post-install hook `_anno1800_set_language_english()` edits
  `%Documents%\Anno 1800\config\engine.ini` (`"TextLanguage":"English"` regex, leaves AudioLanguage)
  — called from `_run_game_mod_install`/`set_game_mod_installed` for `_ANNO_ID`. `GameDetailPanel`'s
  download-mod branch shows a Hebrew note for `game.id==="anno1800"`: the user toggles the language
  once in-game for the full atlas re-bake.
- **Worker:** the slug `anno1800-hebrew` was in `index.js` but **not deployed** (404). Ran
  `npx wrangler deploy` (wrangler was already authed) → `/anno1800-hebrew/manifest` now 200
  (version 1.0.0-beta.1, sha `2fd7ee0d…`), `/archive` 200. The launcher downloads via the Worker,
  so this was a hard prerequisite.
- **Supabase `games` row** brought from `locked`/free to a live paid beta (it had drifted — the
  `mod_version_history` current row was already correct `1.0.0-beta.1`): `price_cents=5300`,
  `availability='available'`, `status='beta'`, `release_stage='beta'`, `version='1.0.0-beta.1'`,
  `download_url`→the release zip, `show_on_*=true`. The website Buy button + the launcher
  "רכישה — 53 ₪" both render now; after a PayPal purchase → `user_purchases` completed →
  `owns_game('anno1800')` → install unlocks. (CP2077 model: the raw GitHub zip stays publicly
  downloadable; payment buys launcher auto-install convenience.)
- **SHIPPED 2026-06-23:** `build_exe.bat` (BUILD_ID `20260623003946`, dev_build 4) → ISCC →
  `publish_release.py 1.0.0 dev` → GitHub `v1.0.0-dev` (asset clobbered) + Supabase
  `launcher_releases` id=41 (is_current). Verified `/api/launcher` buildId `20260623003946`
  (== baked), sha `62e8884a…`, size 247,885,615 B; `/api/games` anno1800 priceCents 5300.


## Anno 1800 Hebrew — groundwork DONE + PROVEN in-game, GO (2026-06-21)

New game scaffolded at `games/anno1800/` (FEASIBILITY.md / RECON.md / PIPELINE.md
+ `work/`). Multi-agent adversarially-verified Phase-1 groundwork, then **both gates
CLOSED by an in-game proof (user-confirmed)**: deploy is the EASIEST in the project
(loose-file mod, no repack, no anti-cheat, keep user locale), Hebrew renders cleanly
in the injected font, and the native HUD is **NON-bidi → store VISUAL** (proven).
Ready for Phase 2 (translation). Memory [[anno1800-groundwork-go]].

- **✅ PROVEN in-game 2026-06-21 (user-confirmed):** a diagnostic mod in
  `Documents\Anno 1800\mods\zzz_hebrew_proof\` flipped main-menu labels. Result: the
  injected **Frank Ruehl font renders Hebrew perfectly** (no tofu), and an A/B on
  working menu GUIDs (New Game 154000 / Options 154002 / Credits 10438) showed
  **VISUAL-stored Hebrew reads correctly while LOGICAL reads reversed** → Anno's
  native HUD does **NOT** do bidi (WD2-menu/AC2 class). So `build_mod.py` defaults to
  **VISUAL** (`visual_line()` = WD2's proven `_visual_line`: Hebrew runs reversed,
  whitespace its own run, Latin/digit/[TOKEN]/%-spec runs intact, run order flipped,
  per line). The translator stores LOGICAL; `visual()` is applied only at build.
- **mod.io mods backed up for fast test launches:** the 189 subscribed mods (~14 GB)
  at `C:\Users\Public\mod.io\4169\mods\` were moved (instant same-drive rename) to
  `…\mods_DISABLED_hebrew_test\` (restore note `RESTORE_MODS_README.txt`; subscriptions
  in `metadata\state.json` untouched). Restore = rename back (game closed). The Hebrew
  mod itself lives in `Documents\Anno 1800\mods\`, loaded fast on its own.

- **Engine/format:** Ubisoft Mainz "Anno" engine; archives = **RDA "Resource File
  V2.2"** (`maindata/data0.rda`..`data33.rda` + per-lang `en_us0/de_de0/fr_fr0/
  ru_ru0.rda`). Format fully cracked; pure-Python READ-ONLY reader **built+proven**
  at `work/rda_reader.py` (seek-only block-chain, zlib-deflate, never loads a multi-
  GB file). Layout: header 0x318 (firstBlockOffset u64 @0x310), 32-byte BlockInfo
  chain at block tails, 560-byte DirEntry (UTF-16LE name[520]+offset+csz+usz+ts+unk).
- **Text spine:** `data0.rda` → **`data/config/gui/texts_english.xml`** (NOT
  en_us0.rda — that's Wwise audio). 12 LTR sibling `texts_<lang>.xml`. Schema UTF-8/
  CRLF: `<TextExport><Texts><Text><GUID>n</GUID><Text>s</Text></Text>…`. **28,165 EN
  records** (measured); numeric `<GUID>` SHARED across all languages = the id-mapping.
  25,167 genuinely localized vs DE; ~4% carry inline markup/placeholders. Scope ~28k
  base, ~1.5–2× with full DLC. UI/content-dominant (buildings/goods/quests/expeditions/
  newspaper/encyclopedia), thin spoken-subtitle tail. Extracted to `extract/`.
- **NO Arabic slot** (declared locales all LTR) → **AC2-class LTR-slot hijack**: ship
  Hebrew inside `texts_english.xml`; user keeps in-game Language=English. `engine.ini`
  `"TextLanguage"`/`"AudioLanguage"` are SEPARATE keys → English VO preserved free.
- **UI engine split (key finding):** the **main HUD is the engine's NATIVE GUI**
  (XML/binary layouts; `data/ui/*.dds` art; rendered with loose `data/fonts/*.ttf`).
  **CEF/Chromium 108.4.13** (`libcef.dll`) drives ONLY the stats/chart/debug web
  panels (d3/nvd3/jquery under `data/config/http`) — those get free ICU bidi; the
  **native HUD is NON-bidi (PROVEN in-game)** → store Hebrew **VISUAL**
  (`build_mod.py` default; `--logical` only for re-testing). Translator output stays
  logical; `visual()` applied only at build.
- **Font:** **NO shipped UI font has Hebrew** (cmap-verified all 15 `data/fonts/*.ttf`
  = Latin+Cyrillic; Meta/Kelvinch/Heuristica/Roboto + CJK). But they're plain loose
  TTFs → injection is LOW complexity (no CR2W-embed / DDS-atlas): `work/anno_font.py`
  ADDS the U+0590–05FF block from a Hebrew source (default Windows `frank.ttf` = Frank
  Ruehl serif, Belle-Époque-fitting; David alt) into each Anno TTF via fontTools
  (DecomposingRecordingPen + TransformPen scale to target upem; preserves the Anno
  font name/Latin/Cyrillic — only Hebrew added), shipped as loose-file `data/fonts/`
  overrides. **Verified offline:** injected metaoffcpro/kelvinch = Hebrew 27/27 +
  Latin 26/26 kept.
- **Deploy = loose-file mod, NO .rda repack.** Mod loading is **built into the game**
  (xforce/anno1800-mod-loader integrated; standalone repo archived) — no DLL to
  install. Drop `mods/<name>/` into **`Documents\Anno 1800\mods\`** (preferred — takes
  precedence, immune to Ubisoft Connect "Verify files", no admin) or `<install>/mods/`.
  Mod = `modinfo.json` + `data/config/gui/texts_english.xml` (ModOps patch: `<ModOp
  Type="add" Path="/TextExport/Texts"><Text><GUID/><Text/></Text>` — adding for an
  existing base GUID overrides it) + `data/fonts/<injected TTFs>` (+ optional scoped
  CSS for the CEF panels). **No anti-cheat** (no EAC/BattlEye). Activation: in-game
  Language=English, restart. Removal = delete folder. RdaConsole/RDAExplorer exist but
  only for one-time READ — we have our own reader, repack never needed.
- **Detection:** `game_detector` ALREADY has `anno1800` patterns + `Anno1800.exe`
  (key == proposed Supabase `games.id` `anno1800`). Single Steam install.
- **work/ tooling:** `rda_reader.py` (reader, proven) · `anno_font.py` (Hebrew TTF
  injection, proven) · `make_proof.py` (builds the proof mod) · `build_mod.py` (full
  mod assembler from `hebrew.json` {guid:he}, `--visual` flag) · `anno1800_{translate,
  watchdog,progress}.py` (SM2-trio adaptation). Precedent: Ukrainian (Nexus 539, RU-slot)
  / Czech (310) / Russian (136) full loose-file translation mods prove the mechanism;
  NO RTL precedent anywhere.
- **Phase 2 (translation) — AGENT HANDOFF BUILT (2026-06-21, user chose the agent path
  over the local-LM trio):** `games/anno1800/agent_handoff/` is the self-contained
  package for a fresh Google/Antigravity agent (per [[delegate-all-translation]] — Claude
  built tooling+glossary+instructions, never translates). Contents: `INSTRUCTIONS.md`
  (filled Hebrew handoff: Belle-Époque register, LOGICAL storage, Anno token rules,
  locked glossary) · `to_translate.json` {guid:en} (28,165) · `skip.json` (718 pure
  data-binding/number records pre-seeded) → **27,447 real translatable** · `hebrew.json`
  ({} output) · helpers `get_batch`/`loop_split`/`loop_merge`/`qa_scan` + `_tokens.py`
  (the KEY Anno bit: a **nesting-aware** token extractor for `<br/>` + nested `[...]`
  data-binds like `[AssetData([RefGuid] Text)]` — preserved verbatim, multiset-validated).
  Loop tooling end-to-end tested (valid merges atomic, dropped-token flagged). The agent
  self-translates in a `get_batch→split→translate→merge` loop to "All done!", writing
  LOGICAL Hebrew. **After the agent finishes:** Claude runs `python work/build_mod.py`
  (VISUAL by default + font inject → `Documents\Anno 1800\mods\zzz_hebrew_translation\`)
  → in-game Language=English → publish like SM2/CP2077 (GitHub `anno1800-hebrew-mods` +
  Worker slug `anno1800-hebrew` + Supabase `games` + `mod_version_history`). (The
  SM2-style local-LM trio `work/anno1800_{translate,watchdog,progress}.py` remains as an
  alternative haul path.)
- **TRANSLATION COMPLETE + MOD BUILT + DEPLOYED (2026-06-21).** Five sequential
  Google/Antigravity agents (no shared history; each handed a fresh self-contained
  resume instruction) ran the loop to "All done!": **27,300 translated + 865 skip =
  28,165 (100% coverage)**. Claude verified independently (GoWR lesson — never trust the
  agent's own merge check): full-spine sweep = **0 token mismatch / 0 foreign / 0 niqqud
  / 0 empty**; 29 internal asset-ID passthroughs (`small_feedback_ship01`,
  `MovieCapture_…`) moved to skip. Built via `work/build_mod.py` → VISUAL + 11
  Hebrew-injected fonts → `Documents\Anno 1800\mods\zzz_hebrew_translation\` (proof mod
  removed). **Two `visual_line` bugs found + fixed by the deployed-XML token sweep (would
  have shipped 1,069 broken binds):** (1) Anno `[...]` data-binds contain SPACES → must
  be **protected as atomic LTR placeholders** before the run-reversal (not split at
  spaces); (2) `<br/>` is a **line break** → split on it like `\n` and visual each segment
  independently (else line order swaps); also top-level non-overlapping tokenizer so a
  `%i` nested inside a `[...]` bind isn't double-extracted. Deployed XML re-verified: 0
  mismatches. **PENDING: user in-game verification** (Language=English, restart) → then
  publish (GitHub `anno1800-hebrew-mods` + Worker slug `anno1800-hebrew` + Supabase
  `games`/`mod_version_history`). The 189 mod.io mods stay disabled until the user restores.

### 🎯🎯 Anno 1800 — THE "IMPOSSIBLE" TARGET IS SOLVED, user-confirmed "עובד!" (later session)

**English slot + FULL Hebrew from COLD BOOT + zero switch + English web content — achieved, pure DATA,
no exe, no Denuvo touched.** The "BLOCKED/IMPOSSIBLE" verdict below is SUPERSEDED (it only proved the
*exe* route was blocked; a data route existed). Winning path = **Hebrew-as-Arabic carrier disguise +
inject fonts INTO the maindata `.rda`**. Detail + universals in memory [[coldboot-atlas-breadth]]:
- **Carrier disguise** (`games/anno1800/work/heb_as_arabic.py`): 27 Hebrew letters → 27 distinct Arabic
  BASE carrier codepoints (exclude alef 0x0627 + lam 0x0644 → no lam-alef ligature); store LOGICAL with
  a **ZWNJ (U+200C) between carriers** (no joining); inject the Hebrew glyph at each carrier. The engine's
  built-in Arabic bidi+shape (pre-baked into the English cold-boot atlas) then draws Hebrew RTL. Untranslated
  GUIDs stay English; web/news follow the English locale → English. `build_arabic_disguise.py` = full build.
- **Inject ALL Arabic joining forms** (init/medi/fina/isol via GSUB single-subst, not just base+isolated)
  or joined runs (labels/paragraphs) render original Arabic; and EMPTY the format-char glyphs
  (U+200C/D/E/F, U+202A-E, U+061C) — the donor ships a visible ZWNJ glyph → thin bars between letters.
- **🔴🔴 THE CRUX: the cold-boot pre-baked atlas bakes from the MAINDATA fonts — a LOOSE-file font override
  reaches only DYNAMICALLY-rendered text (menu/values), NOT pre-baked contexts (setting labels, epilepsy
  warning, profile sub-text).** Symptom: menu Hebrew but labels Arabic, and a further loose-font fix changes
  NOTHING. **Fix = inject the fonts into the maindata `.rda`** (`data4.rda`), exactly as the fan Arabic mod
  did. Pure-Python **RDA writer** `games/anno1800/work/rda_writer.py` (`write_rda_blocks`) — MUST reproduce
  the archive's **exact multi-block layout** (Anno data4 = block1 blacklists + block2 fonts, stored); a
  single-block rebuild OR removing the text `.rda` (`data99`) blanks the whole UI. Keep `data99` (loose ModOp
  overrides it to carriers). Deploy: `games/anno1800/work/deploy_maindata.py` (backs up the fan originals,
  injects fonts into a fan-structure-matching data4, verifies before swap, `--revert`). ⚠️ A game update that
  re-verifies files may overwrite data4 → re-run `deploy_maindata.py`.
- **User's other content**: 189 mod.io mods live in `C:\Users\Public\mod.io\4169\mods\` (disable = rename
  to `mods_DISABLED_hebrew_test`; subscriptions in `metadata\state.json` untouched); the working Hebrew +
  those mods coexist. Restore/working-state doc: `C:\Users\Public\mod.io\4169\RESTORE_MODS_README.txt`.
- **UNIVERSAL: on a prebaked-atlas engine, font work for pre-baked UI must land in the container the atlas
  bakes from (maindata), not a loose override — and a hand-written archive must match the engine's exact
  block layout, or the game rejects it (silent tofu/blank).**

#### Final refinements (later session, user-confirmed) — editable names + the remaining 97 lines + typed-Hebrew
- **🔴🔴 EDITABLE ENTITY NAMES MUST STAY IN THE ORIGINAL LANGUAGE — a fixed translation CLOBBERS the user's
  own edits.** Islands, cities/settlements, ships, and countries/regions are things the PLAYER renames (or
  the game auto-generates and the player then edits). A mod that hard-codes Hebrew for those GUIDs overwrites
  whatever name the user already gave. **Fix: build an EXCLUSION set and emit the ORIGINAL ENGLISH for those
  GUIDs** (`games/anno1800/work/editable_names_exclude.json` = 1,263 GUIDs; `..._english.json` = their EN
  values; the builder emits English for an excluded GUID, never the Hebrew carrier). **Derive the set from
  `assets.xml` (289 MB master GUID→Template map), not by guessing:** ship templates
  (`WarShip`/`TradeShip`/`WarAirShip`/`WorldMapShip`/`SimpleVehicle`/`QuestVehicle`), session/region templates
  (`SessionArctic`/`SessionModerate`/`SessionModerateRandom`/`SessionSouthAmerica`/`Region`), plus the generic
  `Text` template distinguished by its internal `<Name>` field matching precise entity-name patterns
  (`(?i)\bcity ?name\b` / `\bship ?name\b` / `\bisland ?name\b|playersfirstisland|mainislandname`). ⚠️ The
  `<Name>` comes AFTER the `<GUID>` in the XML — capture the FIRST `<Name>` after each GUID. Keep the ISLE
  pattern PRECISE (a loose `island ?name` caught quest prose "Remove the ruined church…"). Result: 0 prose
  caught, 1,250 names → English. **UNIVERSAL for any game: a name the user can rename must stay in its source
  language — never ship a fixed translation for it, or every install clobbers the player's edits.**
- **🔴 Typed Hebrew in a name field renders MIRROR (LTR), not RTL — unreachable by a data mod.** After mapping
  the real Hebrew block (U+05D0-05EA → the carrier glyphs) in the fonts, typing Hebrew into a rename field DOES
  show the letters (not "????"), but the engine lays them LEFT-TO-RIGHT because the RTL layout logic for LIVE
  keyboard input lives in the protected exe (Denuvo), not in any data file. Cannot be fixed via a data mod.
  **Decision (user "ב"): keep the U+05xx→carrier cmap mapping (letters appear) and DOCUMENT the mirror in the
  mod description** — the user types names in English for correct order, or accepts the mirrored look. Both
  this and the editable-names-English behavior are written into `modinfo.json` Description (EN + HE).
- **The 97 genuine remaining lines (outside the original 56k corpus) are translated.** Found by scoping the
  full English source vs the corpus: 84 player-facing → Hebrew (`build_quest_hebrew.py` = 42 New-World/Isabel
  Sarmento campaign narrative paragraphs, preserving literal `<b>`/`<br/>` markup + consistent names
  Isabel Sarmento=איזבל סרמנטו / Sir Archibald=סר ארצ'יבלד / Samuel Goode=סמואל גוד / La Corona=לה קורונה;
  `make_remaining_hebrew.py` = 40 controller/gamepad hints [button labels kept Latin] + misc UI + Enbesa
  quest lines), 12 dev/test/dummy placeholders correctly LEFT English, 1 ship name → English. Merged into the
  build by `build_arabic_disguise.py` (ADD-only, never clobbers a corpus value). **User-authorized one-time
  direct translation** (bounded set, override of [[delegate-all-translation]]). Nothing player-facing remains
  untranslated — the campaign cutscene narrative WAS the last subtitle-like content.
- **🔴 KEYBOARD-KEY LABELS MUST STAY LATIN — the Controls rebind screen showed Hebrew keys (2026-07-30,
  user screenshots).** Anno's dedicated **`999001-999101` "Keys" text pool** (named keys, digits, A-Z,
  F-keys, symbols) + `999256-999259` is where the rebind screen reads key labels; the fleet had translated
  the ones that are English WORDS — **arrow keys `999015-018` (Left/Up/Right/Down → שמאלה/ימינה/למעלה/למטה)
  and keypad `999061-075` ("לוח מקשים N")** — so those bindings rendered Hebrew where a key should be
  (letters A-Z were `HE=None` → English, which is why I/Y/K rendered correctly all along). Fix =
  `work/keyboard_keys_exclude.json` (the pool, derived from the game's own contiguous 999xxx block) +
  `build_arabic_disguise` emits the real English label for those GUIDs (like the editable-names exclusion,
  but pulling English from `to_translate.json`). **⚠️ Excludes ONLY the key pool — `999260+` are the
  crash-reporter categories (Bug/Feedback/Audio/Graphics/Controls…) and error text, legit Hebrew, must NOT
  be touched; and a blanket `EN=='Delete'/'Up'/'Enter'` exclusion is WRONG (those GUIDs are UI buttons
  elsewhere → מחק/למעלה is correct there). The dedicated pool is the precise, safe unit.** UNIVERSAL: this
  is the same "translate labels, never key/code VALUES" rule as GTA/SignalRGB/Borderless — a game's
  key-binding display pool is content-adjacent but must stay in the physical key's own language.
- **🔴 THE `ת/ץ/ף` IN THE SCREENSHOT (rotate-left/right, demolish) COME FROM THE USER'S WINDOWS HEBREW
  KEYBOARD LAYOUT, NOT THE MOD — a data mod cannot change them.** Those keys are the physical comma/period/
  semicolon, whose label GUIDs (`999092`=',' `999094`='.') are already English in the mod (`HE=None`); the
  game resolves an unnamed **punctuation** key via the OS layout's produced character, and under the Israeli
  layout comma→ת, period→ץ, semicolon→ף. So the fix above corrects the letter/named/arrow/keypad keys; the
  punctuation keys read Hebrew because Windows produces Hebrew for them (switch to an English keyboard layout
  to see the Latin punctuation). **UNIVERSAL: a rebind screen can be a HYBRID — friendly-name GUIDs for
  letters/named keys, OS-layout character for punctuation. The OS-layout half is outside any text mod; say so
  instead of chasing it.**
- **Every long dash normalized to a plain hyphen (user request 2026-07-30 "כל '—' תהפוך ל '-'").**
  `build_arabic_disguise._dash()` replaces em (U+2014, 2,780 in the corpus), horizontal-bar (U+2015, 3) and
  en (U+2013, 32) → `-` on EVERY emitted line (Hebrew before carrier-remap, English fallback, kept-English
  labels). Build-time + non-destructive to source (re-runnable), matching how the other transforms work.
  Verified: deployed XML has **0** of all three long-dash codepoints.
- **🔴 THE EDITABLE-NAME EXCLUSION WAS TOO BROAD — FIXED SESSION/REGION names SHOULD be translated
  (user 2026-07-30: "אפשר לתרגם את שמות הארצות כי הם לא משתנים וא"א לערוך אותם?").** The criterion is
  **EDITABILITY, not the word "name"**: the region-selection TAB labels — **The Old World / The New World /
  The Arctic / Enbesa / Cape Trelawney** — are FIXED (`Session*`/`Region` templates, the player can NEVER
  rename them) → they must be **translated**; only genuinely renameable ISLANDS/cities/ships stay English.
  The earlier exclusion had over-included the session/region display GUIDs. Fix = `work/region_names_
  translate.json` (9 GUIDs: 180023/5000000 Old World · 180025/5000001 New World · 180045/160001 Arctic ·
  112132/114327 Enbesa · 110934 Cape Trelawney) subtracted from `exclude` at build time → their corpus
  Hebrew (העולם הישן / העולם החדש / האזור הארקטי / אנבסה / קייפ טרלוני) now shows. **Renameable islands
  stay English** (verified: 112147 Crown Falls, 12018 Bright Sands, 105494 Manola all still English —
  Crown Falls is the settle-able ISLAND *inside* the Cape Trelawney REGION, so the region translates but
  the island doesn't). **UNIVERSAL: "editable name → keep source language" is gated on whether the PLAYER
  can rename THAT field — a fixed world/session/region label is content and must be translated; only the
  user-renameable instance (island/city/ship) is protected. When over-excluded, un-exclude the fixed
  display GUIDs, don't widen the protection.**
- **✅✅ STATE: USER-CONFIRMED WORKING IN-GAME (2026-07-30, "העברית עובדת מצויין").** Loose mod
  rebuilt + deployed to `Documents\Anno 1800\mods\zzz_hebrew_translation` = 55,201 Hebrew(as-carrier)
  + 1,241 kept-English(editable islands/cities/ships) + **42 kept-English (keyboard keys)** + 2,595
  English-fallback = 59,079 GUIDs (9 fixed region/session names moved editable→Hebrew; the 42
  keyboard-key GUIDs that HAD a Hebrew value moved Hebrew→English; the other 63 key GUIDs were already
  `HE=None`→English via fallback). The maindata `data4.rda` fonts are injected (27/27). **189 mod.io
  mods RESTORED + active** (`C:\Users\Public\mod.io\4169\mods\`, 189 folders; the disable was a
  folder-rename only, subscriptions in `metadata\state.json` untouched throughout). Local only; the
  published beta.1 (Korean/English-toggle build) is superseded for THIS user by the English-slot
  carrier build and is NOT re-published (publish only on "פרסם").
- **🔴🔴 THE GARBLING ("רעש וריק" — scrambled Hebrew letters on pre-baked screens) WAS A maindata↔loose
  CARRIER-MAPPING MISMATCH, fixed by a FRESH FULL REGEN.** The Steam `data4.rda` had been injected in an
  OLDER code version whose carrier→Hebrew mapping differed from the current loose fonts/text, so every
  pre-baked screen (descriptions, profile) mapped each Arabic carrier to the WRONG Hebrew letter →
  scrambled Hebrew (dynamic-render screens were fine because they use the current loose fonts). **Fix =
  regenerate EVERYTHING fresh from the raw Noto base with ONE code version:** `cp fan_data4.rda → Steam
  data4` → `deploy_maindata.py` (fresh Hebrew inject) → `build_arabic_disguise.py` (fresh loose). Verified
  offline before the in-game test: **maindata carrier outlines == loose carrier outlines BYTE-IDENTICAL**
  (RecordingPen, 1,889 glyphs each) and **0 non-carrier raw-Arabic values** (the stale deploy had ~150 —
  from before the 313 Arabic-only fixes were in `hebrew.json`). **UNIVERSAL: on a carrier-disguise engine
  where the pre-baked atlas (maindata) and the dynamic fonts (loose) both draw the SAME text, they MUST be
  built from ONE code version — a mapping drift between them scrambles ONLY the pre-baked screens (looks
  like a per-screen bug, not a global one). Always verify maindata↔loose carrier outlines byte-identical
  before an in-game test.**
- **🔴 THE "self-contained font from the game's OWN Latin base" FAILED (`?????`) — it lacks GDEF; the
  engine's Arabic shaper REQUIRES it.** User chose (AskUserQuestion) a from-scratch font built on the
  game's Latin Meta base (`heb_font_clean.py` / `deploy_selfcontained.py`) to avoid any bytes from the
  community mod. It rendered `?????` even after matching cmap/OS-2-bit13/head.flags and dropping the (1,0)
  Mac cmap subtable, because the game's Latin Meta font has **NO Arabic OpenType infrastructure** — the
  Arabic bidi/shaping pipeline needs `GDEF` (+ GPOS/GSUB) and the from-scratch build had no GDEF. A
  from-scratch Arabic-capable font would have to author a full GDEF/GPOS/GSUB, not just cmap + joining
  features. **🔑 AND THE LICENSING CONCERN IS MOOT: the community mod's fonts ARE Noto Naskh Arabic
  (OFL)** — both `metaoffcpro-norm.ttf`/`metaserifoffcpro-medium.ttf` decode as Noto Naskh Arabic /
  Medium (name ID 1, license ID 13 = SIL Open Font License, vendor google.com/get/noto), a free font
  freely redistributable with attribution. So for distribution, **bundle Noto Naskh Arabic (OFL, with
  attribution)** as the carrier-disguise base — no from-scratch font, no fan-mod IP. `heb_font_clean.py`
  is kept as a reference dead-end; the shipping base is the fan/Noto `data4.rda` fonts.

### Anno 1800 — PUBLISHED v1.0.0-beta.1 + the cold-boot verdict (2026-06-22) — SUPERSEDED by the SOLVE above

**🎯 THE USER'S TARGET (recorded verbatim; the "not achieved" here is SUPERSEDED — see the SOLVE above):** Anno 1800 set
to **English** (English menu + English live web content), showing **full, correct Hebrew from COLD BOOT,
with NO language switch at all** — i.e. the exact render the user gets after live-switching TO English,
but persistent from launch without any manual action. **Status (at the time): BLOCKED via the exe route only.** The
language→atlas-breadth decision IS plaintext x86 in `.text` (verified: found the language array
English=0…Korean=9/Italian=10 + the `TextLanguageInitialised` init fn @0x140227522 — so "packed ⇒ code
not there" was wrong), BUT `Anno1800.exe` runs **active Denuvo** (320 MB RWX `.text1`, `.xtls` entry,
Denuvo strings) which **integrity-checks the exe at launch** → any static byte-patch makes the game
refuse to boot. Making an exe edit "stick" therefore requires **defeating Denuvo's integrity check =
DRM circumvention / cracking**, which is **out of scope and will not be done** (a runtime memory-patch
has the same Denuvo-anti-tamper problem). RE helper left at `c:\tmp\anno_re.py` (read-only; VA↔off map,
string find, vectorized RIP-relative xref scan, capstone disasm) for any FUTURE build that ships
**without Denuvo** (older Denuvo games sometimes get it removed in a late patch — if a future
`Anno1800.exe` has no `.text1`/`.xtls`/Denuvo strings, the static patch becomes viable: find the CJK
range-check (lang index in 6..9) near the text-init fn and force it always-broad). Until then, the only
legitimate end-states are the two shipped below (A = Korean slot, full Hebrew no switch; B = English +
one toggle/launch, which the user chose).

**SHIPPED.** Full Hebrew UI mod published. GitHub `hebrew-translation-hub/anno1800-hebrew-mods` FULL
release `v1.0.0-beta.1` (releases/latest) — `anno1800_hebrew.zip` (23,113,143 B, sha
`2fd7ee0da670a2487ff8876fc9596f5ce9a993aa33f384376372ab0fd67c8c0a`) + manifest.json. Packer
`games/anno1800/pack_and_release.py` zips the deployed loose-file folder `zzz_hebrew_translation`
(+ a Hebrew readme documenting both activation paths). Supabase synced: `publish_version.py
anno1800 1.0.0-beta.1 --stage beta --sha … --size 23113143 --archive-url …/v1.0.0-beta.1/anno1800_hebrew.zip
--apply` (games.version+release_stage + mod_version_history is_current) PLUS a direct UPDATE
`games.status='beta'` + `download_url`→the zip (publish_version leaves status/download_url alone).
Worker slug `anno1800-hebrew` added to `games/steam/steam_mod_worker/src/index.js` — **needs
`npx wrangler deploy` (user's CF token); NOT needed for the website Download button** (download_url
→ GitHub directly). All 4 surfaces sha-consistent. 4 news drafts pushed.

**THE COLD-BOOT VERDICT — ⚠️ SUPERSEDED (the "IMPOSSIBLE" was wrong; see the SOLVE at the top of the
Anno section).** The 3-agent scan below was right that the *atlas breadth* can't be flipped in data
AND right that the exe is Denuvo-locked — but it MISSED that the English cold-boot atlas ALREADY
pre-bakes the Arabic script, so disguising Hebrew as Arabic carriers rides it with no breadth change
at all. Keep this as the record of what was ruled out; do NOT act on the "impossible" conclusion.
The user wanted "English setting + full Hebrew at cold-boot + NO language switch." **Was thought
IMPOSSIBLE** by 3 parallel deep-scan agents: the font-atlas glyph BREADTH at cold-boot is decided by the active language's
CLASS inside the **PROTECTED/packed `Anno1800.exe`** (320 MB writable `.text1`, entry in `.xtls`,
~12 KB `.reloc`), not in any editable data. CJK language (Korean/JP/CN/TW) → engine forces a
BROAD/dynamic atlas that preloads Hebrew; English/LTR → NARROW static atlas, omits Hebrew. **No
data lever exists** — PHXLF per-language records are byte-identical CJK-vs-English except name+font
GUID; the PHXFT KIND flag is per-FONT and flipping it 0→1 already gave "????" AND is logically
refuted (Korean→Meta(flag0) STILL boots broad → breadth follows LANGUAGE, not font); properties.xml
`<Languages>` has only audio/locale fields; no charset/glyph-preload config anywhere. Static exe
patch ~10% (packed) → dead. The only two physically-possible end states: **(A) Korean slot** = full
Hebrew cold-boot, no switch, but menu label "한국어" + live CEF web panels (mod.io browser, news
ticker) show Korean; **(B) English slot** = English menu + English web content, full Hebrew only
AFTER one in-game language toggle/launch (a live re-init fires the broad dynamic re-bake). Live
web/news content follows TextLanguage and CANNOT be decoupled by a mod.

**User chose (B) — English + one toggle/launch.** `engine.ini` TextLanguage="English",
AudioLanguage="English". The shipped mod fills ALL 5 slots (chinese/taiwanese/japanese/korean/
english) with the Hebrew + 2,595 English-fallback GUIDs → 100% coverage (no "????" untranslated
leak on any slot), so a downloader can use EITHER path (readme documents both). `build_phxlf`
(Korean→Meta) kept (harmless on English; gives narrow Latin if toggling via Korean). Rebuilt +
verified 2026-06-22: 56,400 Hebrew + 2,595 EN-fallback = 58,995 GUID overrides, **0 real token
mismatches** (23 flagged are correct `SublineText="…"` attr translations — Enbesa/Kashta DLC
sublines; stored LOGICAL inside the atomic tag → may render mirror in that DLC, ~0.04%, minor
follow-up), 16 Hebrew-injected fonts, 4 PHXLF. **Follow-ups:** user confirms (B) in-game (launch
English → toggle language once → full Hebrew); `npx wrangler deploy` for the slug; restore the 189
mod.io mods when done testing (rename `C:\Users\Public\mod.io\4169\mods_DISABLED_hebrew_test\` →
`…\mods\`, game closed). Memory [[anno1800-groundwork-go]].

**Anno operational quick-ref.** Run BOTH scripts with the repo venv (`anno_font` needs fontTools —
base Python313 does NOT have it): `"<repo>/.venv/Scripts/python.exe" work/build_mod.py` (rebuild +
deploy to `Documents\Anno 1800\mods\zzz_hebrew_translation\`; `--logical` only for re-testing bidi,
`--out <dir>` to stage elsewhere) then `… pack_and_release.py <ver> [--pack-only]` (zips the DEPLOYED
folder → `games/anno1800/release/`). The user's active language lives in
`Documents\Anno 1800\config\engine.ini` → `"Gameplay - default values"` → `TextLanguage` /
`AudioLanguage` — a **per-user setting, NOT shipped in the mod**, so publishing never forces a locale.


