# סוכן תיקון 3/4 — Cyberpunk 2077 (fix-only, בלי "OK")

אתה **סוכן 3** מתוך 4. כל שורה ב-`corpus.json` שלך היא תרגום עברי **שבור** —
זוהה אוטומטית כפגום (כתב זר בתוך העברית / עברית+לטינית מודבקות / משפט שנקטע). המשימה:
**לתקן כל שורה** לעברית נכונה וטבעית. **אין כאן "OK" — כל שורה חייבת תיקון אמיתי.**

## ⚠️ בידוד + בלי סקריפטים
- עבוד **רק** בתיקייה: `...agent_handoff_qa\fix_agent_3\`. הרץ רק `fix_get_batch.py` ו-`fix_merge.py`.
- **אסור לכתוב שום סקריפט .py / לולאה / מילון — בשום מקום** (תיקייה/אב/שורש/scratch). fix_merge יחסום.
  קרא את `fix_batch.json` ישירות ותקן ידנית, שורה-שורה.

## הלולאה (עד "QA done!")
  א. `python fix_get_batch.py` → `QA done!` = סיימת. אחרת נוצר `fix_batch.json` = 25 שורות שבורות.
     לכל שורה: `key`, `en` (המקור), `he` (העברית השבורה), `defect` (סוג הפגם), `hint` (רמז).
  ב. תקן **כל** שורה: כתוב עברית נכונה שמתקנת את הפגם:
     - `foreign` → תרגם את המילה הזרה לעברית.
     - `seam` → הפרד/תקן את התעתיק (כמו 'גילherme' → 'גילהרמה').
     - `truncated` → השלם את המשפט מתוך ה-`en`, מלא ונכון.
  ג. כתוב `fix_fixes.json` = `{ "key": "העברית המתוקנת", ... }` — ערך לכל key בבאטץ'.
  ד. `python fix_merge.py`. `REJECT` → תקן רק אותו והרץ שוב (fix_merge בודק שהפגם **באמת נעלם**).
  ה. חזור ל-א'. עד "QA done!".

## כללי-ברזל
- **שמור tokens מילה-במילה:** `<Rich>`/`<kiroshi>`/`{VALUE}`/`%d`/`&rlm;` — העתק בדיוק. רק הטקסט העברי משתנה.
- **בלי ניקוד. רק עברית** (מותגים/קודים לטיני). סדר לוגי רגיל (אל תהפוך — סקריפט נפרד מטפל ב-RTL).
- שמות אנשים/מקומות → עברית עקבית; מותגים (Arasaka/Militech) → לטיני; **V בפרוזה → V**.

### גלוסר
| EN | עברית | EN | עברית |
|---|---|---|---|
| Night City | נייט סיטי | choom | צ'ום |
| eddies/eds | אדיס | gonk | גונק |
| Ripperdoc | ריפרדוק | Netrunner | נטראנר |
| Cyberware | סייברוור | Braindance | ברייןדאנס |
| Arasaka/Militech (מותגים) | נשארים לטיני | V (בפרוזה) | נשאר V |

## סיום
כש-`fix_get_batch.py` = `QA done!`: `--- FIX DONE (סוכן 3) --- תוקנו: X --- END ---`
