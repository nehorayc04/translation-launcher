## UX polish batch + website profile split/votes/donate (2026-07-13, launcher LOCAL build 20260713123818 NOT published; website DEPLOYED live)

A rapid multi-request UX round. **All launcher-frontend + backend fixes built into `build_exe.bat` → latest
BUILD_ID `20260713151600` (superseding `…134501`/`…131939`/`…123818`), exe ~9.2 MB, ISCC →
`Output\TranslationManager-Setup-1.0.2.exe` (243 MB); LOCAL install pending the user's UAC (installer
`PrivilegesRequired=admin`).**

- **🔔 NOTIFICATIONS CENTER + background-activity store (`frontend/src/lib/notifications.ts` +
  `components/NotificationsBell.tsx`).** A framework-agnostic singleton store + `useSyncExternalStore` hook.
  (a) **Fixes the "download progress disappears" bug** — the launcher self-update download progress used to
  live in `DownloadsView`'s `SelfUpdatePanel` LOCAL state, so leaving the screen mid-download lost it and the
  panel showed "עדכן עכשיו" again while the download silently kept running then suddenly installed. Now the
  store subscribes ONCE at App level (`initNotifications()` in `App.tsx`) to `onLauncherUpdateProgress` +
  `onModProgress`, keeps the live activity, and `SelfUpdatePanel` derives from `useActivity('launcher-update')`
  → returning to the screen shows the ONGOING download (the UAC-dismissed 30s watchdog also moved into the
  store, global). (b) **A bell ABOVE the avatar** in `Sidebar.tsx` with 3 states (has-new / none / muted;
  glyphs customizable) + an unread badge, opening a **small FLOATING window** anchored beside it that lists
  live background activities (with progress bars) + system notifications, with **mute** (persisted
  localStorage) + dismiss/clear + mark-read. (c) **Admin-controlled** = the store pulls the public `news`
  feed (`api.getNews()`, editable from the admin site → shown to every user) as system notifications, plus
  app-generated "עדכון תרגום זמין" notices (`pushNotif`). `tsc -b` clean.
- **🎨 Icon manager expanded to 4 groups + 3,933-icon Flaticon library (`icon-manager.html`, gen by
  `build_icon_manager.py`).** Groups: **קיימים** (15 launcher icons, replace) · **פס חלון** (3 window controls
  min/max/close, for the future frameless titlebar) · **התראות** (3 notification-bell states) · **ללא אייקון**
  (**72** icon-LESS UI spots — buttons/headers/empty-states — found by a 5-agent parallel Workflow scan of the
  frontend). Two-pane RTL: left = a searchable/scrollable library of ALL 3,933 Flaticon "Interface icons ·
  Regular Rounded" (CDN webfont); right = 93 target slots across the 4 tabs, assign by drag OR click, plus
  per-slot SVG upload. Export → `icon-changes.json` ({id,group,action:'add'|'replace',iconName/newSvg}).

- **🌊 Liquid-wave progress bar** — the launcher's `ProgressDashboard` simple `h-2` bar was replaced with the
  SAME rate-driven `LiquidWave` (SVG + rAF, leading edge the surface follows, gray "remaining" continuation,
  bubbles + glowing marker) ported **verbatim** from the website `ProgressDashboard`. `frontend/src/components/
  ProgressDashboard.tsx`: `<LiquidWave pct=… ratePerMin={(ratePerHour)/60} primary="#fff700" secondary="#00ffe0"
  height={72}/>`. Frozen while hidden/off-screen; static under `reduce-anims`. No new deps.
- **🧩 Admin-managed plugin catalog (DDL-free via site-config).** The launcher's "תוספים" list is now edited from
  the ADMIN site — the DB Postgres ports are blocked here, so instead of a new `plugins` TABLE the catalog lives
  in the existing `site_config.data.plugins` JSON (public `GET /api/config`, 30s-cached; admin `PATCH` merges it).
  (a) `translation_manager/plugins/registry.py` `_fetch_cloud_catalog` now reads `/api/config → config.plugins`
  (was the Worker slug) + `available()` honors an admin `available:false` (hide). (b) NEW admin
  `website/src/components/admin/PluginsTab.tsx` (add/edit/delete a plugin's id/kind/name/tagline/description/
  version/icon/accent/available/sortOrder, saved via `PATCH /api/config {patch:{plugins}}`). (c) `AdminLayout.tsx`
  nav slot `updates`→`plugins` (label "עדכוני תוכנה"→"תוספים", icon 🧩, renders `PluginsTab`; `UpdatesTab`/`/api/
  updates` stay in the codebase, just unlinked from the nav). (d) `SiteConfig` type +`plugins?[]`. (e) SEEDED
  `config.plugins` with the save-backup entry via the service key (direct `site_config` PATCH). Verified: the
  launcher registry `available()` returns the save-backup plugin from the live `/api/config`. **A plugin shows
  only if the build's host runs its `kind` (SUPPORTED_KINDS={save_backup}) AND `available` — future plugin
  metadata/availability is now admin-controlled with NO app rebuild.**
- **🎨 Icon manager tool `icon-manager.html`** (repo root; open locally; generated by `build_icon_manager.py` from
  `uicons_map.json`). Catalogs all 15 user-facing launcher icons (nav rows + settings tabs + avatar/lock/folder)
  with each one's exact current SVG, Hebrew name, purpose, and `file → Component` location, and — per the user's
  ask to "expand the library ×100, all of Flaticon's Interface-icons Regular" — embeds the **FULL Flaticon UICONS
  "Interface icons · Regular Rounded" set = 3,933 icons** (via the CDN webfont `cdn-uicons.flaticon.com/2.6.0/
  uicons-regular-rounded/css/...`, so it needs internet; the 3,933 class-names + codepoints are parsed from that
  CSS and embedded). Two-pane RTL layout: left = a searchable/scrollable library of all 3,933; right = the 15
  launcher slots as **drop targets**. Assign by DRAG (icon→slot) OR click-a-slot-then-click-an-icon (both work
  while scrolling), plus per-slot "העלה SVG". "ייצוא שינויים" → `icon-changes.json` where each change is either
  `{iconType:'flaticon-uicons-regular-rounded', iconName, unicode}` (a UICONS pick) or `{iconType:'svg-upload',
  newSvg}`. **To APPLY the user's picks later:** for a UICONS pick, extract that glyph's SVG outline from the
  woff2 (`cdn-uicons.flaticon.com/2.6.0/uicons-regular-rounded/webfonts/uicons-regular-rounded.woff2`) via
  fontTools (the `unicode` field pins the glyph), convert to a stroke/size matching the target component, and
  drop it into the launcher; for an svg-upload, use `newSvg` directly. Regenerate the HTML: re-run
  `build_icon_manager.py`. (JS node --check clean.) Website changes DEPLOYED
`vercel --prod` + **promoted** + verified LIVE. **⚠️ Vercel deploy gotchas this session:** (1) `vercel --prod`
in this profile creates a STAGED production deployment that must be **`vercel promote <url>`**d (409 "already
current" is fine); (2) the live `ProfilePage` chunk hash ≠ the local build hash — that's just Vercel's build
env, NOT a stale deploy → **verify code-is-live by grepping the live chunk for a NEW string, not by comparing
hashes** (I chased a false "404 / old build" until I grepped the chunk and found "ההצבעות שלי"/"הספרייה שלי").

- **🔴 VOTES bug ROOT-CAUSED + FIXED (`auth/manager.py get_votes`).** The launcher personal area always showed
  "0 הצבעות" even for an account that HAD votes (verified: nc52885@gmail.com/a2e5db97 voted plague-tale yet the
  launcher read 0). **Root: the 2026-07-01 security hardening ran `revoke select on public.votes from anon,
  authenticated`** to close a direct-read leak, but the launcher read votes with the USER's own access token
  (the `authenticated` role) via PostgREST → permission-denied → `[]`. **Fix: read votes via the HUB API**
  (`GET https://hebrew-translation-hub.com/api/games?action=votes` with the bearer token → `myVotes`), the
  sanctioned service-role read path — NO direct PostgREST `/votes` query, NO DB change. (An alternative DB-only
  fix `website/supabase/votes_launcher_read_fix.sql` — self-scoped `authenticated` SELECT `auth.uid()=user_id`
  + regrant — was written but **NOT applied: this environment blocks outbound Postgres ports 5432/6543 to the
  Supabase pooler**, so the psycopg2 migration path is unavailable here; the launcher-API fix supersedes it and
  needs no DB access. If ever wanted server-side for OTHER clients, run that SQL in the Supabase SQL editor.)
  **Account note: the "voted but shows 0" was a REAL bug once the launcher was on the same account that voted —
  earlier I misdiagnosed it as an account mismatch when the launcher happened to be on dwd64801 (0 votes).**
- **Splash preload STRENGTHENED (`App.tsx`).** The home still "popped in" images after the splash lifted. The
  preload now mirrors HomeView's exact `featured` selection (flagged-sorted, else the FALLBACK_IDS) and
  preloads those banners + covers + the top-14 covers, and waits on **`img.decode()`** (bitmap paint-ready,
  stronger than `onload`) before flipping `imagesReady`; cap 5s, splash `maxMs` 6s. So the splash stays until
  the first-screen images are DECODED → they paint the instant it lifts (warm 2nd launch = instant via cache).

- **Games nav icon** (`NavIcons.tsx` `LibraryIcon`) — the old short/wide pad read squished at 20px; replaced
  with Lucide **gamepad-2** (fills the 24×24 box y5→19, balanced D-pad + face buttons).
- **View-transition between menus** (`index.css` `.view-transition` + `@keyframes view-in`) — a fade+rise+
  micro-scale played on a **keyed remount**. `App.tsx` wraps the main content in `<div key={contentKey}
  className="h-full view-transition">` (`contentKey` = loading/g:id/s:id/v:view); `SettingsView` tab content
  upgraded from `animate-fade-in`→`view-transition`. **fill-mode = `backwards` NOT `both`** so the transform
  is REMOVED after ~340ms → a `position:fixed`/`sticky` descendant (modals, detail rail) is never pinned to a
  transformed ancestor (the 100% frame == base, so the revert is seamless). Auto-off under `reduce-anims`.
- **Grid gap** in games (`LibraryView`) + software (`AppsView`) `gap-5`→`gap-3`.
- **VirtualDJ activation note removed** (`GameDetailPanel.tsx`): the shared native-DL note (`isGowr &&
  installed`, where `isGowr` = "any single-flow native applier", INCLUDING VirtualDJ) is now gated
  `&& game.id !== "virtualdj"`; also `שאר המשחק`→`שאר {noun}`.
- **Launcher personal area split games/software** (`PersonalAreaView.tsx`): `PurchasesSection` gained a
  `kind:"games"|"software"` prop (title + empty copy); the parent splits `purchases` by
  `row.games?.is_software` into "המשחקים שלי" + "התוכנות שלי" (software panel hidden when the user owns no
  software). Needs the flag on the purchase join → **`manager.py` purchases `select` += `is_software`** +
  `eel.ts` `MyPurchase.games.is_software?`.
- **Plugins moved from Settings → a top-level nav item AFTER "הורדות ועדכונים"** (user). `Sidebar.tsx` `NavKey`
  += `"plugins"` + a `PluginsIcon` (puzzle) + NAV row (`accent #a78bfa`); `App.tsx` renders `<PluginsSettings>`
  when `view==="plugins"`; `SettingsView` DROPPED its plugins tab (TABS/TabKey/render/import/`IconPlugins` all
  removed). `PluginsSettings` root → a full-height `h-full overflow-y-auto` view. Plugin card button
  **"הפעל"→"הורדה"** (`p.enabled ? "כבה" : "הורדה"`).
- **Website `/profile` (`ProfilePage.tsx`, LIVE):** tab "המשחקים שלי"→**"הספרייה שלי"** now splits into
  labelled **משחקים** + **תוכנות** groups (`LibraryGroupHeader`; select += `is_software`; `OwnedGame.isSoftware`).
  NEW **"ההצבעות שלי"** tab (`MyVotesTab`) = the user's votes via `useVotes()` `mine` → game covers/titles from
  a `games` `.in('id', votedIds)` query, cards link to `/vote`. (The launcher already had a votes section; the
  user's "voted but shows 0" was CORRECT — verified their uid `deec0572…` has 0 rows in `votes`; the vote
  simply never persisted / different account.) Donate section (Ko-fi + PayPal-when-set) already on game+software
  pages.
- **"Which games on the launcher home" is ALREADY admin-controlled** — the `games.featured` flag (admin GamesTab
  ★ toggle / "מוצג בתרגומים מובילים") drives the launcher HomeView `FeaturedCarousel` (reads `g.featured` live via
  `_shape_supabase_game` + `select=*`, no rebuild). Only improved the ★ tooltip to say "דף הבית של התוכנה".

- **DEFERRED to the next round (LARGE, communicated):** (1) **admin-managed plugins** — turn the plugin catalog
  DB-driven: a Supabase `plugins` table + `/api/plugins` (public GET + admin CRUD) + repurpose the admin
  "עדכוני תוכנה" (`UpdatesTab`) slot into a Plugins manager (title/description/version/available/update) + point
  the launcher `registry.py`/`get_plugins` at the API. (2) **liquid-wave progress bar** — port the website
  `ProgressDashboard` `LiquidWave` into the launcher's progress UI (HomeView/GameDetailPanel).

---


## Site copy → "Google agents" (translation method change, 2026-07-01)

Translation no longer uses local LM models (gemma/qwen on the AMD RX 9070) — it's now **Google
agents (Gemini family) via Antigravity**. Swept EVERY public surface so nothing still claims a
local model:

- **Code (deployed `vercel --prod`):** `src/data/faq.ts` (the "איך התרגום נעשה" answer → Google
  Gemini agents through Antigravity + auto-QA; the accuracy answer dropped "Gemma-2 27B" → "מודלי
  שפה ענקיים של Google (Gemini)"; donation line "חשמל/GPU" → "תשתית/שירותי AI/פיתוח"),
  `src/pages/AboutPage.tsx` (tech card "Local AI Models" → **"Google AI Agents"** + Gemini/
  Antigravity blurb; SEO description), `src/lib/phaseLabels.ts` (rate label "קצב עיבוד GPU / שעה"
  → "שורות מתורגמות / שעה"), `src/components/admin/ProgressEditor.tsx` (GPU field placeholder →
  "(ענן — לא בשימוש)"). **Intentionally LEFT:** faq.ts "קובצי הטקסט המקומיים" = local FILES (not a
  model), and historical `news`/`updates` entries (a dated record, not rewritten).
- **DB (the real "still shows gemma" culprit):** the home **ProgressDashboard** renders
  `ai_model · gpu_model` from Supabase `progress_snapshots` — NOT from code. All 5 rows still held
  `gemma-4-31b-it`/`Qwen2.5 32B` + `AMD RX 9070`. PATCHed every row (service key, PostgREST) →
  `ai_model='Google Gemini'`, `gpu_model=''` (no local GPU — cloud). Verified live `/api/progress`
  = 0 gemma/RX-9070/Qwen. This surfaces on BOTH the website dashboard and the launcher home
  progress card (both read the same snapshots) — no launcher rebuild needed. **Lesson: after a
  translation-method change, the model shown to users lives in `progress_snapshots` (DB), not just
  the React copy — update both or the dashboard keeps showing the old model.**

---


